# ═══════════════════════════════════════
# Telegram Bot Configuration
# ═══════════════════════════════════════

# Bot token from @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather_here

# Encryption key for securing user private keys in database (32-byte hex string)
# Generate a secure random key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TELEGRAM_ENCRYPTION_KEY=your_64_character_hex_encryption_key_here

# Your Telegram user ID (get from @userinfobot)
TELEGRAM_OWNER_ID=your_telegram_user_id_here

# ═══════════════════════════════════════
# Important Notes
# ═══════════════════════════════════════
# - All ORB mining configuration is stored in the DATABASE
# - Each user manages their own settings via /settings command
# - RPC endpoints, SOL amounts, thresholds, etc. are all database-managed
# - This .env file only contains telegram bot authentication/security settings
